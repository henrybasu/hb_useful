# Copy to .env and fill in free-tier keys.
# Finnhub is exposed to the client bundle (VITE_*).
VITE_FINNHUB_KEY=

# GNews is used only at build time (scripts/fetch-headlines.mjs).
# Prefer GNEWS_KEY; VITE_GNEWS_KEY still works as a fallback.
GNEWS_KEY=
